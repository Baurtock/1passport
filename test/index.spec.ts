import { index } from '../src/index';

import { expect } from 'chai';

describe("test", () => {
    it("should return true", () => {
        expect(index('test')).to.equal(true);
    });
});


